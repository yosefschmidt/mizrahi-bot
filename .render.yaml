services:
  - type: web
    name: mizrahi-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: starter
    
    envVars:
      - key: EMAIL_COMPONEY
        sync: false
      - key: EMAIL_PASS
        sync: false
      

