services:
  - type: web
    name: mizrahi-bot
    env: node
    buildCommand: ./render-build.sh
    startCommand: npm start
    plan: starter
    envVars:
      - key: EMAIL_COMPONEY
        sync: false
      - key: EMAIL_PASS
        sync: false
